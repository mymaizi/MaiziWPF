<UserControl
    x:Class="MaiziWPF.Modules.Sys.IconPickerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:core="clr-namespace:MaiziWPF.Core;assembly=MaiziWPF.Core"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:local="clr-namespace:MaiziWPF.Modules.Sys"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVis" />
        <core:StringToPackIconKindConverter x:Key="StringToPackIconKindConverter" />
        <Style x:Key="IconGridButton" TargetType="Button">
            <Setter Property="Width" Value="56" />
            <Setter Property="Height" Value="56" />
            <Setter Property="Margin" Value="8" />
            <Setter Property="Background" Value="#f5f5f5" />
            <Setter Property="BorderBrush" Value="#dcdfe6" />
            <Setter Property="BorderThickness" Value="1.5" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="OverridesDefaultStyle" Value="True" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="12">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="BorderBrush" Value="#1890ff" />
                    <Setter Property="Background" Value="#e6f7ff" />
                </Trigger>
                <DataTrigger Binding="{Binding}" Value="{Binding DataContext.SelectedIcon, RelativeSource={RelativeSource AncestorType={x:Type local:IconPickerView}}}">
                    <Setter Property="BorderBrush" Value="#1890ff" />
                    <Setter Property="Background" Value="#e6f7ff" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid>
        <ToggleButton
            x:Name="PART_ToggleButton"
            Width="36"
            Height="36"
            Background="#f5f5f5"
            BorderBrush="#dcdfe6"
            BorderThickness="1"
            Cursor="Hand"
            IsChecked="{Binding IsOpen}"
            ToolTip="选择图标">
            <ToggleButton.Style>
                <Style TargetType="ToggleButton">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="ToggleButton">
                                <Border
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="4">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#e6f7ff" />
                            <Setter Property="BorderBrush" Value="#91d5ff" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </ToggleButton.Style>
            <materialDesign:PackIcon
                Width="20"
                Height="20"
                Foreground="#606266"
                Kind="{Binding SelectedIcon, Converter={StaticResource StringToPackIconKindConverter}}" />
        </ToggleButton>

        <Popup
            AllowsTransparency="True"
            IsOpen="{Binding IsOpen, Mode=TwoWay}"
            Placement="Bottom"
            PlacementTarget="{Binding ElementName=PART_ToggleButton}"
            StaysOpen="False"
            VerticalOffset="4">
            <Border
                Width="520"
                Height="460"
                Background="White"
                BorderBrush="#e0e0e0"
                BorderThickness="1"
                CornerRadius="12">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" Opacity="0.15" ShadowDepth="5" />
                </Border.Effect>
                <Grid Margin="16,12,16,12" Background="White">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBox
                        Grid.Row="0"
                        Height="40"
                        Margin="0,0,0,10"
                        FontSize="13"
                        Text="{Binding SearchText, Delay=300, UpdateSourceTrigger=PropertyChanged}"
                        materialDesign:HintAssist.Hint="搜索图标"
                        materialDesign:TextFieldAssist.HasClearButton="True" />

                    <Grid Grid.Row="1" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox
                            Grid.Column="0"
                            Height="40"
                            FontSize="13"
                            Text="{Binding ManualInput}"
                            materialDesign:HintAssist.Hint="例如：Account" />
                        <Button
                            Grid.Column="1"
                            Margin="8,0,0,0"
                            Padding="20,8"
                            Background="#07bf96"
                            BorderBrush="#07bf96"
                            Command="{Binding UseManualCommand}"
                            Content="使用"
                            Foreground="#fff"
                            FontSize="13"
                            Style="{StaticResource MaterialDesignRaisedLightButton}" />
                    </Grid>

                    <Grid Grid.Row="2">
                        <ProgressBar
                            Width="200"
                            Height="4"
                            IsIndeterminate="True"
                            Visibility="{Binding IsLoading, Converter={StaticResource BoolToVis}}"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Style="{StaticResource MaterialDesignLinearProgressBar}" />
                        <TextBlock
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            FontSize="14"
                            Foreground="#999"
                            Text="请输入关键词搜索图标">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding IsLoading}" Value="False" />
                                                <Condition Binding="{Binding FilteredIcons.Count}" Value="0" />
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Visibility" Value="Visible" />
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="ScrollChanged">
                                        <prism:InvokeCommandAction Command="{Binding ScrollChangedCommand}" />
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                                <ScrollViewer.Style>
                                    <Style TargetType="ScrollViewer">
                                        <Setter Property="Visibility" Value="Visible" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding FilteredIcons.Count}" Value="0">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding IsLoading}" Value="True">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </ScrollViewer.Style>
                                <ItemsControl ItemsSource="{Binding FilteredIcons}">
                                    <ItemsControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel />
                                        </ItemsPanelTemplate>
                                    </ItemsControl.ItemsPanel>
                                    
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Border
                                                Width="56" Height="56"
                                                Margin="5"
                                                CornerRadius="5"
                                                Cursor="Hand"
                                                ToolTip="{Binding}">
                                                <Border.Style>
                                                    <Style TargetType="Border">
                                                        <Setter Property="Background" Value="#f5f5f5" />
                                                        <Setter Property="BorderBrush" Value="Transparent" />
                                                        <Setter Property="BorderThickness" Value="1.5" />
                                                        <Style.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#e6f7ff" />
                                                                <Setter Property="BorderBrush" Value="#1890ff" />
                                                            </Trigger>
                                                        </Style.Triggers>
                                                    </Style>
                                                </Border.Style>
                                                <i:Interaction.Triggers>
                                                    <i:EventTrigger EventName="MouseLeftButtonDown">
                                                        <prism:InvokeCommandAction
                                                            Command="{Binding DataContext.SelectIconCommand, RelativeSource={RelativeSource AncestorType={x:Type local:IconPickerView}}}"
                                                            CommandParameter="{Binding}" />
                                                    </i:EventTrigger>
                                                </i:Interaction.Triggers>
                                                <materialDesign:PackIcon
                                                    Kind="{Binding}"
                                                    HorizontalAlignment="Center"
                                                    VerticalAlignment="Center" />
                                            </Border>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                    </Grid>
                </Grid>
            </Border>
        </Popup>
    </Grid>
</UserControl>