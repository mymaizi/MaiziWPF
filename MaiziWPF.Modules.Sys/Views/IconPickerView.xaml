<UserControl
    x:Class="MaiziWPF.Modules.Sys.IconPickerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:core="clr-namespace:MaiziWPF.Core;assembly=MaiziWPF.Core"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Width="680" Height="520">
    <UserControl.Resources>
        <core:StringToPackIconKindConverter x:Key="StringToPackIconKindConverter" />
        <Style x:Key="IconGridButton" TargetType="Button">
            <Setter Property="Width" Value="48" />
            <Setter Property="Height" Value="48" />
            <Setter Property="Margin" Value="4" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="#404040" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1a1a2e" />
                    <Setter Property="BorderBrush" Value="#1890ff" />
                </Trigger>
                <DataTrigger Binding="{Binding}" Value="{Binding DataContext.SelectedIcon, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}">
                    <Setter Property="Background" Value="#1890ff" />
                    <Setter Property="BorderBrush" Value="#1890ff" />
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="20,16,20,16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!--  搜索框  -->
        <TextBox
            Grid.Row="0"
            Height="36"
            Margin="0,0,0,12"
            FontSize="13"
            Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
            materialDesign:HintAssist.Hint="搜索图标"
            materialDesign:TextFieldAssist.HasClearButton="True" />

        <!--  手动输入  -->
        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox
                Grid.Column="0"
                Height="36"
                FontSize="13"
                Text="{Binding ManualInput}"
                materialDesign:HintAssist.Hint="例如：mdi:account-circle-outline" />
            <Button
                Grid.Column="1"
                Margin="8,0,0,0"
                Padding="16,8"
                Background="#1890ff"
                BorderBrush="#1890ff"
                Command="{Binding UseManualCommand}"
                Content="使用"
                Foreground="#fff"
                Style="{StaticResource MaterialDesignRaisedLightButton}" />
        </Grid>

        <!--  图标网格  -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding FilteredIcons}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button
                            Command="{Binding DataContext.SelectIconCommand, RelativeSource={RelativeSource AncestorType={x:Type UserControl}}}"
                            CommandParameter="{Binding}"
                            Style="{StaticResource IconGridButton}"
                            ToolTip="{Binding}">
                            <materialDesign:PackIcon
                                Width="24"
                                Height="24"
                                Foreground="#cccccc"
                                Kind="{Binding Converter={StaticResource StringToPackIconKindConverter}}" />
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!--  底部按钮  -->
        <StackPanel
            Grid.Row="3"
            Margin="0,12,0,0"
            HorizontalAlignment="Right"
            Orientation="Horizontal">
            <Button
                Margin="0,0,12,0"
                Padding="24,8"
                Background="#1890ff"
                BorderBrush="#1890ff"
                Command="{Binding ConfirmCommand}"
                Content="确定"
                Foreground="#fff"
                Style="{StaticResource MaterialDesignRaisedLightButton}" />
            <Button
                Padding="24,8"
                BorderBrush="#dcdfe6"
                Command="{Binding CancelCommand}"
                Content="取消"
                Foreground="#606266"
                Style="{StaticResource MaterialDesignOutlinedDarkButton}" />
        </StackPanel>
    </Grid>
</UserControl>