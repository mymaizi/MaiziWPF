<UserControl
    x:Class="MaiziWPF.Modules.Sys.DictDataFormView"
    x:Name="RootUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:maiziWPF="clr-namespace:MaiziWPF.Core;assembly=MaiziWPF.Core"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:prism="http://prismlibrary.com/"
    prism:ViewModelLocator.AutoWireViewModel="True"
    Width="520" Height="280">
    <prism:Dialog.WindowStyle>
        <Style TargetType="Window">
            <Setter Property="Title" Value="{Binding DialogTitle}" />
        </Style>
    </prism:Dialog.WindowStyle>
    <UserControl.Resources>
        <maiziWPF:BooleanConverter x:Key="EqualToBooleanConverter" />
        <Style x:Key="FormLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="#606266" />
            <Setter Property="VerticalAlignment" Value="Center" />
            <Setter Property="TextAlignment" Value="Right" />
        </Style>
        <Style x:Key="RadioStyle" TargetType="RadioButton">
            <Setter Property="FontSize" Value="13" />
            <Setter Property="Foreground" Value="#606266" />
            <Setter Property="Margin" Value="0,0,24,0" />
        </Style>
    </UserControl.Resources>

    <Grid Margin="0,0,0,8">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="1" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,12,20,12">
            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Style="{StaticResource FormLabel}" Foreground="#ff4d4f" Text="* 字典标签" />
                <TextBox
                    Grid.Column="2"
                    Height="36"
                    FontSize="13"
                    Text="{Binding DictLabel, UpdateSourceTrigger=PropertyChanged}"
                    materialDesign:HintAssist.Hint="请输入字典标签" />
                <TextBlock Grid.Column="3" Style="{StaticResource FormLabel}" Foreground="#ff4d4f" Text="* 字典键值" />
                <TextBox
                    Grid.Column="5"
                    Height="36"
                    FontSize="13"
                    Text="{Binding DictValue, UpdateSourceTrigger=PropertyChanged}"
                    materialDesign:HintAssist.Hint="请输入字典键值" />
            </Grid>

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Style="{StaticResource FormLabel}" Text="字典排序" />
                <TextBox
                    Grid.Column="2"
                    Height="36"
                    FontSize="13"
                    Text="{Binding DictSort, UpdateSourceTrigger=PropertyChanged}"
                    materialDesign:HintAssist.Hint="请输入排序号" />
                <TextBlock Grid.Column="3" Style="{StaticResource FormLabel}" Text="是否默认" />
                <StackPanel Grid.Column="5" Orientation="Horizontal" VerticalAlignment="Center">
                    <RadioButton
                        Content="是"
                        GroupName="IsDefault"
                        IsChecked="{Binding IsDefault, Converter={StaticResource EqualToBooleanConverter}, ConverterParameter=Y}"
                        Style="{StaticResource RadioStyle}" />
                    <RadioButton
                        Content="否"
                        GroupName="IsDefault"
                        IsChecked="{Binding IsDefault, Converter={StaticResource EqualToBooleanConverter}, ConverterParameter=N}"
                        Style="{StaticResource RadioStyle}" />
                </StackPanel>
            </Grid>

            <Grid Margin="0,0,0,12">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="100" />
                    <ColumnDefinition Width="12" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Style="{StaticResource FormLabel}" Text="备注" />
                <TextBox
                    Grid.Column="2"
                    Grid.ColumnSpan="4"
                    Height="80"
                    FontSize="13"
                    AcceptsReturn="True"
                    TextWrapping="Wrap"
                    Text="{Binding Remark, UpdateSourceTrigger=PropertyChanged}"
                    materialDesign:HintAssist.Hint="请输入备注" />
            </Grid>
        </StackPanel>

        <Border Grid.Row="1" Background="#e8e8e8" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="16,12">
            <Button
                Margin="0,0,8,0"
                Padding="24,6"
                Background="#1890ff"
                BorderBrush="#1890ff"
                Command="{Binding AcceptCommand}"
                Content="确定"
                Foreground="#fff"
                Style="{StaticResource MaterialDesignRaisedLightButton}" />
            <Button
                Padding="24,6"
                BorderBrush="#dcdfe6"
                Command="{Binding CancelCommand}"
                Content="取消"
                Foreground="#606266"
                Style="{StaticResource MaterialDesignOutlinedDarkButton}" />
        </StackPanel>
    </Grid>
</UserControl>