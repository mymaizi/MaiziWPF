<ToggleButton
    x:Class="MaiziWPF.Core.DeptTreeSelectControl"
    x:Name="PART_ToggleButton"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    Height="36"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Center"
    Padding="12,0"
    BorderBrush="#dcdfe6"
    Background="White"
    Cursor="Hand">
    <ToggleButton.Template>
        <ControlTemplate TargetType="ToggleButton">
            <Border
                x:Name="border"
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="1"
                CornerRadius="4">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="24" />
                    </Grid.ColumnDefinitions>
                    <TextBlock
                        Grid.Column="0"
                        Margin="8,0,0,0"
                        VerticalAlignment="Center"
                        FontSize="13"
                        Text="{Binding DisplayText, RelativeSource={RelativeSource TemplatedParent}}"
                        Foreground="#303133" />
                    <materialDesign:PackIcon
                        Grid.Column="1"
                        Kind="ChevronDown"
                        Width="16"
                        Height="16"
                        VerticalAlignment="Center"
                        Foreground="#909399" />
                </Grid>
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsChecked" Value="True">
                    <Setter TargetName="border" Property="BorderBrush" Value="#1890ff" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </ToggleButton.Template>
    <Popup
        x:Name="PART_Popup"
        PlacementTarget="{Binding ElementName=PART_ToggleButton}"
        Placement="Bottom"
        StaysOpen="False"
        AllowsTransparency="True"
        IsOpen="{Binding IsChecked, ElementName=PART_ToggleButton}"
        PopupAnimation="Fade">
        <Border
            x:Name="PART_PopupBorder"
            Background="White"
            BorderBrush="#e4e7ed"
            BorderThickness="1"
            CornerRadius="4"
            MaxHeight="280"
            MinWidth="240"
            MaxWidth="360">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="1" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <TreeView
                    x:Name="PART_TreeView"
                    Grid.Row="0"
                    BorderThickness="0"
                    ItemsSource="{Binding ItemsSource, ElementName=PART_ToggleButton}"
                    SelectedItemChanged="PART_TreeView_SelectedItemChanged"
                    ScrollViewer.VerticalScrollBarVisibility="Auto">
                    <TreeView.Resources>
                        <Style TargetType="TreeViewItem">
                            <Setter Property="IsExpanded" Value="True" />
                            <Setter Property="FontSize" Value="13" />
                            <Setter Property="Foreground" Value="#303133" />
                            <Setter Property="Padding" Value="4,2" />
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#f5f7fa" />
                                </Trigger>
                                <Trigger Property="IsSelected" Value="True">
                                    <Setter Property="Background" Value="#e6f7ff" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </TreeView.Resources>
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Childs}">
                            <TextBlock Text="{Binding DeptName}" VerticalAlignment="Center" />
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
                <Separator Grid.Row="1" Background="#e4e7ed" />
                <Button
                    Grid.Row="2"
                    Content="清除选择"
                    Height="32"
                    Margin="8,4"
                    Padding="12,0"
                    HorizontalAlignment="Center"
                    FontSize="12"
                    Foreground="#909399"
                    BorderBrush="Transparent"
                    Background="Transparent"
                    Click="ClearSelection_Click"
                    Style="{StaticResource MaterialDesignFlatButton}" />
            </Grid>
        </Border>
    </Popup>
</ToggleButton>